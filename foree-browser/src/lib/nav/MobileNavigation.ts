import type { MobileNavigation } from "$lib/types";

export const mobileNavigation: MobileNavigation = [
    {
        subMenuTitle: "Menu",
        defaultActive: true,
        navigations: [
            
        ]
    }
]